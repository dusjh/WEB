메인페이지-방명록 쓰기(write) - 정상입력: 목록으로 - wirte.ok -> DB에서 insert
		      			 - 실패: 경고메시지
	    -상세보기(제목 누르면) - 수정(암호로 확인) -  wirte 페이지로  - 암호 맞으면 상세페이지로/ 암호 틀리면 -그대로(write) ->update.ok -> DB에서 update
			    		   - 삭제 : 암호로 확인 - 암호 맞으면 삭제되고 목록페이지로/ 암호 틀리면 그대로(delete, 암호입력) ->delete.ok -> DB에서 delete